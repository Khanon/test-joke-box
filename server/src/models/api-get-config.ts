export interface ApiGetConfig {
    // This is just an example where headers, validators, and others configs of a call could be placed.
    // In this way a config could be shared between different API calls.
    // This is also a way to separate the config and logical layers.
}